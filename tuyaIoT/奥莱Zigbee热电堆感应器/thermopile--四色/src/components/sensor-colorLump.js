import React, { Component } from 'react';
import { View, Text, StyleSheet, Button, Animated } from 'react-native';

class ColorLump extends Component {
    constructor(props) {
        super(props);
        this.state = {  }
    }
    render() { 
        return ( <View>

        </View> );
    }
}
 
export default ColorLump;